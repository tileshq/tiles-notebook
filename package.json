{
  "name": "tiles",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "version": "1.0.0",
  "description": "Tiles monorepo workspace",
  "scripts": {
    "dev": "npm run dev --workspace=tiles-playground",
    "build": "npm run build --workspace=wasm-runner && npm run build --workspace=tiles-playground",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "confusing-browser-globals": "^1.0.11",
    "eslint-config-fbjs": "^4.0.0",
    "eslint-plugin-lexical": "^0.0.1-security",
    "eslint-plugin-react-hooks": "^5.2.0"
  }
}
