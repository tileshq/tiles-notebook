'use client';

// Note: In Next.js, we typically don't need this component since metadata is handled in layout.tsx or page.tsx
// But we'll keep it for compatibility with the original codebase
export default function DocumentHead(): JSX.Element | null {
  // Next.js handles the head/meta tags in layout.tsx
  return null;
}